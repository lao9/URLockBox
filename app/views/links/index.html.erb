<h1>Your URLockBox</h1>

<div class="link-form well">
  <h3>Add New Link</h3>
  <%= form_for(@link) do |f|  %>
    <div class="form-group">
      <%= f.label :url, "URL" %>
      <%= f.text_field :url, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control' %>
    </div>
    <div class="form-group">
      <%= f.hidden_field :user_id %>
      <%= f.submit "Add Link" %>
    </div>
  <% end %>

</div>

<div class="filters">
  <h3>Filters:</h3>
  <p id="search-filter">By text: <input type="text"></p>
</div>

<hr>

<div class="links row">
  <% @links.each do |link| %>
    <%= render partial: 'links/link', locals: {link: link} %>
  <% end %>

</div>
